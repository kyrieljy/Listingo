# Listingo 四期演示平台实施计划

## 总体目标

构建本机运行的 Vue 3 + FastAPI + SQLite 模块化单体应用。`/app` 提供四期统一工作台，`/admin` 提供运营后台。当前交付目标是让远端仓库能够复现本地正在运行的源码、Prompt 资产、Provider 路由、A+ 0729 规划和视频 Seedance 2.0 链路。

## 已交付阶段

1. 建立前后端骨架、锁定依赖、创建本地 Docker Compose 与交接文档。
2. 实现 SQLite 模型、Alembic 迁移、API Key 加密、Provider seed。
3. 导入商品套图核心 Meta Prompt，建立 Prompt 版本、JSON 契约和 Workflow 注册表。
4. 完成 Dryrun 状态机、真实 LLM/Image Provider、回退、并发和脱敏日志。
5. 完成商品套图工作台、上传、策略确认、进度、结果、失败重试、ZIP/长图下载。
6. 完成单图真实二次编辑、版本历史和任务历史。
7. 完成 A+、视频、Agent 四期统一导航与基础交互。
8. 完成运营后台 Provider、Workflow、Prompt、日志、runtime settings。
9. 按核心 Meta Prompt 规则补齐商品视觉事实、比例变量、语义校验、一次重规划、内容安全和前台确认闭环。
10. 完成视频真实链路：`ecommerce-video-meta-15s`、`shengsuanyun_tasks_generation`、submit + poll + mp4 落盘。
11. 完成 A+ 详情页真实链路：`aplus-meta` 0729、模块数量选择、输出目标、A+ Web/移动端 Provider route。
12. 完成后台 Prompt 上传、版本比较、LLM 试跑、完整链路试跑和 `is_admin_test` 隔离。
13. 完成 Provider route role：`llm`、`suite_fidelity`、`suite_layout`、`aplus_detail`、`aplus_mobile`、`video`。
14. 将本地当前生效的 7 类 Prompt 资产、源码、测试、文档同步到 Git。

## 当前运行原则

- 全局默认 Dryrun，未配置有效 Provider 与密钥时不得外调。
- 当前运行 Prompt 资产共 7 类：`ecommerce-meta`、`aplus-meta`、`product-vision`、`copywriting-assist`、`edit-rewrite`、`content-safety-review`、`ecommerce-video-meta-15s`。
- `aplus-meta` 使用 `backend/app/prompts/aplus_meta_prompt_0729.md`。
- `image-quality-review` 只作为可能存在的历史数据库记录处理，当前源码不再调用。
- Provider seed 当前共 8 个模型配置，Live 选择以 route role 为准。
- 图片单任务并发默认 4，可通过 `LISTINGO_MAX_JOB_CONCURRENCY` 在 1-8 间调整。
- 视频使用 `shengsuanyun-doubao-seedance-2-0` / `bytedance/doubao-seedance-2-0`，要求公开可访问的 `LISTINGO_PUBLIC_ASSET_BASE_URL`。
- 每个任务固定引用创建时的 Prompt/Workflow 版本，后台启用新版本不影响历史任务。

## 验收范围

- 后端：API、Prompt 契约、Provider route、A+ 模块解析、视频任务 payload、内容安全、seed、迁移、历史过滤。
- 前端：工作台四期入口、A+ 模块计数、视频表单、后台 Provider 分组、Prompt 上传/测试交互。
- 文档：`README.md`、`TECH_STACK.md`、`SPEC.md`、`开发计划.md`、`TASKS.md`、`design-qa.md` 与当前源码一致。
- Git：提交完整本地版本并推送到 `origin/main`，不提交运行时数据库、密钥、生成产物、缓存或构建产物。

## 提交前验证

```powershell
.\.venv\Scripts\python.exe -m pytest backend\tests
Set-Location frontend
npm.cmd run test:run
npm.cmd run build
Set-Location ..
.\.venv\Scripts\python.exe -m alembic -c backend\alembic.ini heads
```

还需要对 7 个运行时 Prompt 做本地 SQLite active 版本与源码文件哈希对照，确认 push 后 seed 文件就是本地正在跑的提示词内容。
