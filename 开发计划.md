# Listingo 四期演示平台实施计划

## 总体目标

构建本机运行的 Vue 3 + FastAPI + SQLite 模块化单体应用。`/app` 为四期演示工作台，`/admin` 为亮色运营后台。第一期提供真实模型链路与完整资产管理；二至四期提供高完成度交互演示。全局默认 Dryrun。

## 分阶段交付

1. 创建前后端骨架、锁定依赖，并生成五份交接文档。
2. 实现 SQLite 模型、Alembic 迁移、加密密钥和五个预置模型配置。
3. 导入核心 Meta Prompt，完成版本管理、JSON 契约和 Workflow 注册表。
4. 实现 Dryrun 状态机、真实 LLM/Image Provider、回退、并发和安全日志。
5. 完成一期 DesignKit 风格工作台、上传配置、进度、结果和失败重试。
6. 完成单图真实二次编辑、版本历史、任务历史和批量 ZIP。
7. 完成二至四期交互 Demo 和统一四期导航。
8. 完成模型、Workflow、提示词和日志后台。
9. 完成自动化测试、Live 冒烟测试入口、视觉对比、响应式修复与交接文档复核。
10. 按核心 Meta Prompt 的内部规则补齐商品视觉事实、结构化输入、比例变量、语义审查/一次重规划、生成后 QA、辅助提示词版本资产与前台确认/失败重试闭环。

## 实施原则

- 前端按 1440×900 DesignKit 工作台结构复刻，替换为 Listingo 自有品牌与素材；390×844 使用抽屉而非横向溢出。
- Meta Prompt 原文版本化保存，运行时追加 JSON 合约。
- 一期只接受 `#@` 生图路由，单任务并发上限 3。
- LLM JSON 失败只修复一次；商品保持优先时图片单项按 Nano Banana Pro → Nano Banana 2 回退一次，视觉排版优先时直接使用 Image 2。
- 图片接口按能力路由：Nano Banana Pro/Nano Banana 2 使用可携带参考图的原生 `generateContent`；Image 2 纯文生图使用 generations，带参考商品图使用 multipart edits。
- 每完成或失败一项都更新 `TASKS.md` 并报告原因与下一步。

## 验收范围

- 自动化覆盖输入校验、Prompt 合约、Dryrun 零外调、Provider 回退、版本、ZIP、密钥加密、Workflow 校验和日志脱敏。
- 浏览器验证四期入口、后台四模块、1440×900 与 390×844。
- Live 冒烟测试只由有效密钥显式启动，不进入默认 CI。

完整业务规则、调用链路与异常处理以 [SPEC.md](SPEC.md) 为唯一依据。
