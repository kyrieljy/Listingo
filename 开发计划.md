# Listingo 四期平台实施计划

## 总体目标

构建本机/私有化运行的 Vue 3 + FastAPI + SQLite 模块化单体应用。`/app` 提供电商创意生产工作台，`/admin` 提供运营后台。当前交付目标是让远端仓库能够复现本地正在运行的源码、Prompt 资产、Provider 源码目录、默认路由、A+ 0729 规划、Seedance 2.0 视频链路、账号/套餐、批量托管、OCR 改字和 AI 水印下载能力。

## 已交付阶段

1. 建立前后端骨架、锁定依赖、创建本地 Docker Compose 与交接文档。
2. 实现 SQLite 模型、Alembic 迁移、API Key 加密、Provider seed。
3. 导入商品套图核心 Meta Prompt，建立 Prompt 版本、JSON 契约和 Workflow 注册表。
4. 完成 Dryrun 状态机、真实 LLM/Image Provider、回退、并发和脱敏日志。
5. 完成商品套图工作台、上传、策略确认、进度、结果、失败重试、ZIP/长图下载。
6. 完成单图真实二次编辑、版本历史和任务历史。
7. 完成 A+、视频、Agent 四期统一导航与基础交互。
8. 完成运营后台 Provider、Workflow、Prompt、日志、runtime settings。
9. 按核心 Meta Prompt 规则补齐商品视觉事实、比例变量、语义校验、一次重规划、内容安全和前台确认闭环。
10. 完成视频真实链路：`ecommerce-video-meta-15s`、Seedance 2.0 route chain、submit + poll + mp4 落盘。
11. 完成 A+ 详情页真实链路：`aplus-meta` 0729、模块数量选择、输出目标、A+ Web/移动端 Provider route。
12. 完成后台 Prompt 上传、版本比较、LLM 试跑、完整链路试跑和 `is_admin_test` 隔离。
13. 完成 Provider route chain：`llm`、`suite_fidelity`、`suite_layout`、`aplus_detail`、`aplus_mobile`、`image_edit`、`video`。
14. 完成登录/注册、账号中心、管理员短信二次验证、通知、套餐、额度和模拟支付订单。
15. 完成批量托管：套图/A+ 批次创建、调度、取消、失败重试、恢复和 ZIP 导出。
16. 完成 OCR 改字：PaddleOCR/RapidOCR 配置、文字行检测、`image-text-edit` Prompt、文字替换版本生成。
17. 完成 AI 水印下载权限和运营监控图表。

## 当前运行原则

- 全局默认 Dryrun，未配置有效 Provider 与密钥时不得外调。
- 当前源码 Prompt 资产共 8 类：`ecommerce-meta`、`aplus-meta`、`product-vision`、`copywriting-assist`、`edit-rewrite`、`image-text-edit`、`content-safety-review`、`ecommerce-video-meta-15s`。
- `aplus-meta` 使用 `backend/app/prompts/aplus_meta_prompt_0729.md`。
- `image-quality-review` 只作为可能存在的历史数据库记录处理，当前源码不再调用。
- Provider 源码目录当前共 47 个预设；本机数据库可保留 5 个 legacy Provider，但它们不属于新源码事实。
- Provider route slot 使用 `primary`、`backup1`、`backup2`、`backup3`、`backup4`；旧 `fallback` 仅作为 `backup1` 兼容别名。
- 图片单任务并发默认 4，可通过 `LISTINGO_MAX_JOB_CONCURRENCY` 在 1-8 间调整。
- 全局 Provider 并发默认 4，可通过 `LISTINGO_MAX_PROVIDER_CONCURRENCY` 调整。
- 批量任务默认 `LISTINGO_MAX_BATCH_TASKS=100`、`LISTINGO_MAX_BATCH_ITEM_ASSETS=6`、`LISTINGO_MAX_ACTIVE_BATCH_ITEMS=1`。
- Live 图片、二次编辑、OCR 改字、A+ 移动端派生和视频都可能需要公开可访问的 `LISTINGO_PUBLIC_ASSET_BASE_URL`。
- 每个任务固定引用创建时的 Prompt/Workflow 版本，后台启用新版本不影响历史任务。
- API Key、短信凭据、Provider 启用状态、运行时 DB、上传、结果、导出和 `.secret_key` 不通过 Git 同步。

## 验收范围

- 后端：API、账号、订阅、额度、Prompt 契约、Provider route、A+ 模块解析、视频任务 payload、内容安全、OCR、seed、迁移、历史过滤。
- 前端：工作台四期入口、账号中心、套餐弹窗、A+ 模块计数、视频表单、批量托管、OCR 改字、后台 Provider 分组、Prompt 上传/测试、短信/OCR/监控交互。
- 文档：`README.md`、`TECH_STACK.md`、`SPEC.md`、`开发计划.md`、`TASKS.md`、`design-qa.md` 与当前源码一致。
- Git：提交完整本地源码版本并推送到 `origin/main`，不提交运行时数据库、密钥、生成产物、缓存或构建产物。

## 提交前验证

```powershell
.\.venv\Scripts\python.exe -m pytest backend\tests
Set-Location frontend
npm.cmd run test:run
npm.cmd run build
Set-Location ..
.\.venv\Scripts\python.exe -m alembic -c backend\alembic.ini heads
```

还需要对 8 个运行时源码 Prompt 做本地 SQLite active 版本与源码文件哈希对照，确认 push 后 seed 文件就是本机正在跑的提示词内容。历史 `image-quality-review` 不参与源码同步。

## 服务器更新原则

本轮更新涉及 `backend/requirements.txt` 和 `frontend/package*.json`。服务器 pull 后需要重新安装依赖、运行 Alembic 迁移并重启服务；Docker Compose 部署需要 rebuild 后端和前端镜像。
